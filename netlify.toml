[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "22"                   
  NODE_OPTIONS = "--max_old_space_size=6144"  


[build.processing]
  skip_processing = false